name: Update website from PPT

on:
  push:
    paths:
      - "ppts/*.pptx"
      - "ppts/**/*.pptx"
      - "scripts/extract_pptx.py"
      - "scripts/check_new_names.py"
      - "scripts/compile_ipo_details.py"
      - "scripts/bundle_standalone.py"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: hk-cornerstone-data-update
  cancel-in-progress: false

jobs:
  update-data:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Rebuild all PPT data
        run: python scripts/extract_pptx.py --force

      - name: Build investor search aliases
        run: python scripts/build_investor_search_aliases.py

      - name: Compile IPO detail placeholders
        run: python scripts/compile_ipo_details.py

      - name: Build website
        run: python scripts/build_site.py

      - name: Commit generated website data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/data.json data/data.js data/.pptx_index.json data/ipo_details.json data/ipo_details.js data/investor_search_aliases.js public/
          git commit -m "Auto-update website data from PPT" || exit 0
          git pull --rebase origin main
          git push origin main
